id = "copilot"
version = "2026.07.07.1"
min_engine_version = 1
updated_at = "2026-07-07T14:15:00Z"
aliases = ["github-copilot", "ghcs"]

# agentmux extension: this dialog can appear before lifecycle hooks are active.
[[rules]]
id = "folder_trust_blocker"
state = "blocked"
priority = 320
region = "whole_recent"
visible_blocker = true
contains = ["confirm folder trust", "do you trust the files"]
any = [
  { contains = ["enter to select"] },
  { contains = ["yes"] },
]

[[rules]]
id = "selection_blocker"
state = "blocked"
priority = 300
region = "whole_recent"
visible_blocker = true
all = [
  { any = [
    { contains = ["esc to cancel"] },
    { contains = ["esc cancel"] },
  ] },
  { any = [
    { contains = ["enter to select"] },
    { contains = ["enter to confirm"] },
    { contains = ["enter to submit"] },
    { contains = ["enter accept"] },
  ] },
]

[[rules]]
id = "working_cancel_hint"
state = "working"
priority = 100
region = "whole_recent"
visible_working = true
any = [
  { contains = ["esc to cancel"] },
  { contains = ["esc cancel"] },
  { contains = ["esc again to cancel"] },
  { contains = ["esc interrupt"] }
]
